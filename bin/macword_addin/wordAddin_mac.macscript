tell application "Microsoft Word"
   save active document
   set originDoc to name of active document
   set tmp_dir to path to temporary items
   tell active document
	
   end tell
end tell

on subroutine(parameter)

end subroutine